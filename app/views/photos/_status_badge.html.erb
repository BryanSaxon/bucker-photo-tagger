<% if photo.complete? %>
  <span class="badge badge--done"><span class="badge__dot"></span>Complete</span>
<% else %>
  <span class="badge badge--warn"><span class="badge__dot"></span>Unprocessed</span>
<% end %>
